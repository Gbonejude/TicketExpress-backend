*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail

# Restes d'upload de spatie/laravel-medialibrary : fichiers temporaires recréés à
# chaque envoi, suivis par erreur jusqu'ici.
/storage/media-library/temp
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Modelling sources (draw.io, PlantUML, PowerAMC, Visual Paradigm).
# Editable design artefacts kept outside the repository; export the rendered
# images into docs/ if a diagram needs to be versioned alongside the code.
*.drawio
*.dio
*.puml
*.plantuml
*.cdm
*.xmi
*.mpd
*.bpd
